name: data
description: "Data layer of Agrost app"
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.2
  shared_preferences: ^2.5.5
  fpdart: ^1.2.0

  # inner packages
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
