name: domain
description: "Domain layer of Agrost app"
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.9.0
  json_annotation: ^4.11.0
  equatable: ^2.0.8
  fpdart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  json_serializable: ^6.13.1
  mockito: ^5.6.4

flutter:
