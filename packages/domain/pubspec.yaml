name: domain
description: "Domain layer of Agrost app"
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  equatable: ^2.0.8
  fpdart: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
  mockito: ^5.6.4
  build_runner: ^2.13.1
